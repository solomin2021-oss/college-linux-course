
### **📄 lessons/02-users-permissions/labs/practice.md**
```markdown
# Практические задания - Урок 2

## 🎯 Задание 1: Создание структуры проекта

Создайте следующую структуру папок и настройте права:

/var/www/
└── college-project/
├── public/ # Доступ на чтение всем
├── private/ # Только владельцу
├── shared/ # Владельцу и группе
└── logs/ # Только владельцу, но можно писать логи


**Требования:**
- Владелец: `developer1`
- Группа: `webdev`
- Настройте соответствующие права для каждой папки

## 🎯 Задание 2: Скрипт автоматизации

Создайте скрипт `setup-project.sh`, который:

1. Создает пользователя `project-admin`
2. Создает группу `project-team` 
3. Добавляет `developer1`, `developer2` в группу
4. Создает папку `/opt/project-files`
5. Настраивает права: владелец `project-admin`, группа `project-team`, права `775`

## 🎯 Задание 3: Анализ прав доступа

Проанализируйте текущие права в системе:

```bash
# 1. Найдите все файлы с правами 777
find / -type f -perm 777 2>/dev/null | head -10

# 2. Найдите все файлы, принадлежащие вашему пользователю
find / -user $(whoami) 2>/dev/null | head -10

# 3. Проверьте права на критичные папки
ls -la /etc/passwd
ls -la /etc/shadow
ls -la /root

🎯 Часто используемые комбинации
Права	Назначение
755	Исполняемые файлы, папки
644	Обычные файлы
600	Конфиденциальные файлы
777	Все права (избегать!)

💡 Памятка по правам
Цифра	Права	Буквы
7	read + write + execute	rwx
6	read + write	rw-
5	read + execute	r-x
4	read only	r--
0	no permissions	---

⚠️ Важные предупреждения
Никогда не используйте chmod 777 на весь систем!

Не работайте постоянно под root

Проверяйте права на конфиденциальные файлы (/etc/passwd, /etc/shadow)

Используйте sudo только когда необходимо