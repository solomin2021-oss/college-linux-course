# Лабораторная работа 2: Пользователи и права доступа

## 📋 Задание
Научиться управлять пользователями, группами и правами доступа в Linux.

## 🛠️ Инструкция

### Часть 1: Работа с пользователями

#### Создание пользователей
```bash
# 1. Создаем трех тестовых пользователей
sudo adduser developer1
sudo adduser developer2  
sudo adduser tester1

# Для каждого пользователя задайте пароль (например, user123)
# Остальные поля можно пропустить (Enter)

Часть 2: Работа с группами
Создание и управление группами
bash
# 1. Создаем группу для разработчиков
sudo groupadd webdev

# 2. Добавляем пользователей в группу
sudo usermod -aG webdev developer1
sudo usermod -aG webdev developer2
sudo usermod -aG webdev admin

# 3. Проверяем членство в группах
groups developer1
id developer1

Просмотр информации о группах
bash
# 4. Просмотр всех групп
cat /etc/group | grep webdev

# 5. Проверка своих групп
groups

Часть 3: Права доступа к файлам и папкам
Создание тестовой структуры
bash
# 1. Создаем папку для проекта
sudo mkdir -p /var/www/college-project

# 2. Меняем владельца и группу
sudo chown developer1:webdev /var/www/college-project

# 3. Проверяем права
ls -la /var/www/

Настройка прав доступа
bash
# 4. Даем полные права владельцу и группе
sudo chmod 770 /var/www/college-project

# 5. Создаем тестовый файл от имени developer1
sudo -u developer1 touch /var/www/college-project/index.html
sudo -u developer1 echo "Hello World" > /var/www/college-project/index.html

# 6. Проверяем права файла
ls -la /var/www/college-project/

Часть 4: Практика с правами
Тестирование доступа
bash
# 1. Пробуем прочитать файл разными пользователями
sudo -u developer1 cat /var/www/college-project/index.html
sudo -u developer2 cat /var/www/college-project/index.html  
sudo -u tester1 cat /var/www/college-project/index.html

# 2. Пробуем записать в файл
sudo -u developer2 echo "Added by dev2" >> /var/www/college-project/index.html
Понимание прав доступа
bash
# 3. Анализ прав доступа (числовой формат)
stat /var/www/college-project/index.html

# 4. Изменение прав (символьный формат)
sudo chmod o+r /var/www/college-project/index.html  # Даем чтение остальным